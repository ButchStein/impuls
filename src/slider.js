import * as $ from 'jquery';

$('.content__slider').slick({
  arrows: false,
  autoplay: true,
  autoplaySpeed: 3000
})
$('.content__banner').slick({
  fade: true,
  arrows: false,
  autoplay: true,
  autoplaySpeed: 5000
})